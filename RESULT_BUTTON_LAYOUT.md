# 결과 화면 버튼 레이아웃 v0.6.2

## 배치

1. `다른 유형 체험`: 대표 행동, 결과 카드 전체 너비
2. `같은 유형 다시 체험` / `시작 화면`: 동일 너비 2열
3. 화면 너비 620px 이하: 보조 버튼을 세로 1열로 전환

## 공통 규칙

- 버튼 최소 높이: 56px
- 모서리 반경: 16px
- 버튼 간격: 12px
- 대표 버튼에만 약한 그림자 적용
- 버튼 ID와 기존 클릭 동작은 변경하지 않음
