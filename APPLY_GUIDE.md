# 적용 가이드 (v0.9.8)

## 핵심 변경
- `src/main.ts`의 O-03(불법주차 차량) 위치를 우측으로 이동
- 연관된 경유 포인트/가이드/펜스/감속영역 동기화

## 확인 방법
1. `npm ci`
2. `npm run dev` 또는 `npm run build`
3. 횡단보도 건넌 뒤 도로 건너편에서 봤을 때 차량이 더 우측에 있는지 확인
